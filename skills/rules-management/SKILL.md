---
name: rules-management
description: 识别和结构化业务规则、判定条件、计算口径、参数、例外和可配置性要求。
keywords:
  - 业务规则
  - 决策表
  - 计算口径
  - 参数
  - 例外
---

# 工作流 (Workflow)

1. 读取 RR、需求澄清产物、竞品参考和已生成上游产物，重点检查 `ambiguity-map.md`、`decision-records.md` 和 `requirement-maturity-assessment.md` 中的澄清状态。
2. 抽取业务规则、触发条件、判定条件、结果、例外和优先级。
3. 将复杂条件转成决策表，标注冲突、缺口和默认处理。
4. 输出可配置参数，说明默认值、范围、生效条件和变更影响。
5. 回读产物，确认规则编号、来源和状态稳定。

# Tool Usage Notes

- 使用 `extract_lookup_values` 辅助提取枚举、状态、字典和阈值候选。
- 不生成流程、单据字段或技术接口细节，只引用必要上下文。
- 规则无法确认时记录为待确认，不补成确定规则。
- “待确认”项不得写成确定规则；“按假设推进”项必须标注假设来源、适用边界和下游风险。

# 最终生成策略 (Final Generation)

1. 每条规则要包含编号、名称、触发条件、判断逻辑、输出结果、例外、优先级、来源、确认状态和推进假设/待确认说明。
2. 决策表要覆盖关键条件组合和冲突处理。
3. 参数 YAML 要保持字段稳定，便于 IR 聚合和后续 IT 设计复用。
